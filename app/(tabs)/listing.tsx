import { Text, StyleSheet } from 'react-native';
import { Screen } from '@/components/Screen'; import { SectionCard } from '@/components/SectionCard'; import { FormField } from '@/components/FormField'; import { ScreenshotPicker } from '@/components/ScreenshotPicker'; import { useSubmission } from '@/hooks/useSubmission'; import { colors } from '@/constants/theme';
export default function Listing(){const {draft,update}=useSubmission();return <Screen><Text style={styles.title}>Store listing</Text><Text style={styles.lede}>Create listing copy and attach local assets.</Text><SectionCard title="Listing copy"><FormField label="Promotional text" value={draft.promotionalText} onChangeText={promotionalText=>update({promotionalText})} multiline/><Text style={styles.count}>{draft.promotionalText.length} characters · suggested maximum 170</Text><FormField label="Description" value={draft.description} onChangeText={description=>update({description})} multiline/><FormField label="Keywords" value={draft.keywords} onChangeText={keywords=>update({keywords})}/><Text style={styles.count}>{draft.keywords.length} / 100 characters</Text><FormField label="Support email" value={draft.supportEmail} onChangeText={supportEmail=>update({supportEmail})} keyboardType="email-address" autoCapitalize="none"/></SectionCard><SectionCard title="Assets"><ScreenshotPicker screenshots={draft.screenshots} documents={draft.documents} onChangeScreenshots={screenshots=>update({screenshots})} onChangeDocuments={documents=>update({documents})}/></SectionCard></Screen>}
const styles=StyleSheet.create({title:{color:colors.text,fontSize:27,fontWeight:'800',marginTop:8},lede:{color:colors.muted,marginVertical:10},count:{color:colors.muted,fontSize:12,marginTop:-8,marginBottom:14}});
