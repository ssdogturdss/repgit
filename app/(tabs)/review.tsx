import { Text, StyleSheet } from 'react-native';
import { Screen } from '@/components/Screen'; import { SectionCard } from '@/components/SectionCard'; import { FormField } from '@/components/FormField'; import { useSubmission } from '@/hooks/useSubmission'; import { colors } from '@/constants/theme';
export default function Review(){const {draft,update}=useSubmission();return <Screen><Text style={styles.title}>App Review</Text><Text style={styles.lede}>Provide the information your release manager will enter in App Store Connect.</Text><SectionCard title="Review contact"><FormField label="Name" value={draft.reviewContactName} onChangeText={reviewContactName=>update({reviewContactName})}/><FormField label="Email" value={draft.reviewContactEmail} onChangeText={reviewContactEmail=>update({reviewContactEmail})} keyboardType="email-address" autoCapitalize="none"/><FormField label="Phone" value={draft.reviewContactPhone} onChangeText={reviewContactPhone=>update({reviewContactPhone})} keyboardType="phone-pad"/></SectionCard><SectionCard title="Review instructions"><FormField label="Demo account instructions" value={draft.demoAccountInstructions} onChangeText={demoAccountInstructions=>update({demoAccountInstructions})} multiline/><FormField label="Notes for reviewer" value={draft.reviewerNotes} onChangeText={reviewerNotes=>update({reviewerNotes})} multiline/></SectionCard></Screen>}
const styles=StyleSheet.create({title:{color:colors.text,fontSize:27,fontWeight:'800',marginTop:8},lede:{color:colors.muted,marginVertical:10}});
