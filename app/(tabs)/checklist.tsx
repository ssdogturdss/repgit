import { Text, StyleSheet } from 'react-native';
import { Screen } from '@/components/Screen'; import { SectionCard } from '@/components/SectionCard'; import { ChecklistRow } from '@/components/ChecklistRow'; import { checklistItems } from '@/constants/checklist'; import { useSubmission } from '@/hooks/useSubmission'; import { colors } from '@/constants/theme';
export default function Checklist(){const {draft,toggleChecklist}=useSubmission();return <Screen><Text style={styles.title}>Release checklist</Text><Text style={styles.lede}>Required tasks affect local readiness. Optional tasks are retained for handoff completeness.</Text><SectionCard title="Apple-side release work">{checklistItems.map(item=><ChecklistRow key={item.id} item={item} checked={draft.checklist[item.id]} onPress={()=>toggleChecklist(item.id)}/>)}</SectionCard></Screen>}
const styles=StyleSheet.create({title:{color:colors.text,fontSize:27,fontWeight:'800',marginTop:8},lede:{color:colors.muted,marginVertical:10,lineHeight:20}});
