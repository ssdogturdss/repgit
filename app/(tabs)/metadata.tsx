import { Text, StyleSheet } from 'react-native';
import { Screen } from '@/components/Screen'; import { SectionCard } from '@/components/SectionCard'; import { FormField } from '@/components/FormField'; import { useSubmission } from '@/hooks/useSubmission'; import { colors } from '@/constants/theme';
export default function Metadata(){const {draft,update}=useSubmission(); return <Screen><Text style={styles.title}>App metadata</Text><Text style={styles.lede}>Identity and release details for your local submission package.</Text><SectionCard title="App identity"><FormField label="App name" value={draft.appName} onChangeText={appName=>update({appName})}/><FormField label="Bundle ID" value={draft.bundleId} onChangeText={bundleId=>update({bundleId})} autoCapitalize="none"/><FormField label="SKU" value={draft.sku} onChangeText={sku=>update({sku})}/><FormField label="Primary category" value={draft.primaryCategory} onChangeText={primaryCategory=>update({primaryCategory})} placeholder="e.g. Productivity"/></SectionCard><SectionCard title="Release"><FormField label="Version" value={draft.version} onChangeText={version=>update({version})}/><FormField label="Build number" value={draft.buildNumber} onChangeText={buildNumber=>update({buildNumber})}/><FormField label="Release notes" value={draft.releaseNotes} onChangeText={releaseNotes=>update({releaseNotes})} multiline/></SectionCard><SectionCard title="URLs"><FormField label="Privacy policy URL" value={draft.privacyPolicyUrl} onChangeText={privacyPolicyUrl=>update({privacyPolicyUrl})} keyboardType="url" autoCapitalize="none"/><FormField label="Support URL" value={draft.supportUrl} onChangeText={supportUrl=>update({supportUrl})} keyboardType="url" autoCapitalize="none"/><FormField label="Marketing URL (optional)" value={draft.marketingUrl} onChangeText={marketingUrl=>update({marketingUrl})} keyboardType="url" autoCapitalize="none"/></SectionCard></Screen>}
const styles=StyleSheet.create({title:{color:colors.text,fontSize:27,fontWeight:'800',marginTop:8},lede:{color:colors.muted,marginVertical:10}});
