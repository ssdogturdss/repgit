# Do not store Apple credentials, certificates, provisioning profiles, or App Store Connect API keys in this app.
# Expo public configuration variables, if introduced, must be safe to embed in a client build.
EXPO_PUBLIC_APP_ENV=development
